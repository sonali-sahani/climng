- ADD PACKAGE.JSON
-install few necessary packages(express,dotenv,body-parser,mongoose,nodemon)
-create a basic node server(env file creation-declare port,)
-create config 
-create models directory
  -add a single model for patient
  -add a few datatypes in the json object ->string,datatypes
  -call the model in route
-add a new route
   -all books route
   -basic skeleton
      -crud method
      -mongo function call
      -show in fronted correct output
      -respond with a json and 404 error
        -if successful show a domy output on the respective path/route  